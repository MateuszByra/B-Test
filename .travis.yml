sudo: required
services:
    - docker

before_install:
    - docker build -t yeasy/simple-web:test -t yeasy/simple-web:latest -f ./Dockerfile .

script:
    - docker run -e CI=true yeasy/simple-web:test

deploy:
  provider: heroku
  api_key:
	  secure: $"HEROKU_API_TOKEN"
  on:
	  branch: master